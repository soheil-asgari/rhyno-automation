@model dynamic
<div class="empty-state text-center py-4">
    <i class="bi @Model.Icon fs-2 d-block mb-2"></i>
    <div class="fw-semibold">@Model.Title</div>
    <div class="small text-muted">@Model.Description</div>
</div>
