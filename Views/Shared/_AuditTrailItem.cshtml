@model dynamic

<div class="audit-item">
    <div class="audit-meta">
        <strong>@Model.Title</strong>
        <span class="small text-muted">@Model.When</span>
    </div>
    <div class="small">@Model.Detail</div>
    <div class="small text-muted">@Model.Source</div>
</div>
